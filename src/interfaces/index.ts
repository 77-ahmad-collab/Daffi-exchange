export type TabBoxComponentProps = {
  tabs: {
    name: string;
  }[];
};

export type CardProps = {
  title: string;
  secondaryTitle: string;
  subTitle: string;
  imageSrc: string;
  link: string;
  backgroundColor: string;
};
